/* eslint-disable no-console */
'use strict';

const hobbit = {
  meals: ['breakfast', 'second breakfast', 'elevenses', 'lunch', 'afternoon tea', 'dinner', 'supper'],
};

console.log(`Fourth meal of the day is ${hobbit.meals[3]}`);